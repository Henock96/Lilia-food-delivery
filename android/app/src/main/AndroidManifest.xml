<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Suivi pendant une course, écran éteint.

         Sans service de premier plan, le `Timer` et le flux GPS de l'app sont
         suspendus dès qu'elle quitte l'écran : le livreur roulait, le client
         voyait un marqueur figé. Un service de premier plan avec notification
         persistante est la façon dont Android autorise ce suivi, et la seule
         qui soit honnête vis-à-vis du livreur — il voit en permanence que sa
         position est transmise, et le service s'arrête avec la course.

         ⚠️ `ACCESS_BACKGROUND_LOCATION` n'est délibérément **pas** demandée :
         un service de premier plan de type `location` reçoit les positions
         sans elle. La demander imposerait au livreur un second dialogue
         système (« Toujours autoriser », hors de l'app sur Android 11+) et
         une justification supplémentaire au Play Store, pour une capacité
         dont on n'a pas besoin — on ne suit jamais un livreur hors course. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Android 14+ : refuse de démarrer un service `location` sans ce type. -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="Lilia Food Delivery"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Clé Google Maps — remplacer YOUR_GOOGLE_MAPS_API_KEY par la vraie clé -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${googleMapsApiKey}"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="high_importance_channel" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@android:color/transparent" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>

        <!-- Android 11+ (API 30) filtre ce qu'une application peut « voir » des
             autres. Sans ces déclarations, `canLaunchUrl` renvoie false et les
             appels ci-dessous échouent SANS erreur : le bouton paraît
             simplement mort. C'était le cas des boutons « Appeler » et « SMS »
             de l'écran de course — un livreur qui ne trouvait pas la porte ne
             pouvait pas joindre le client. -->

        <!-- ⚠️ L'action doit être VIEW, et pas DIAL / SENDTO : `url_launcher`
             construit un `Intent(Intent.ACTION_VIEW)` aussi bien pour
             `canLaunchUrl` que pour `launchUrl` (url_launcher_android,
             UrlLauncher.java). Une déclaration DIAL ne débloque donc rien —
             elle a l'air correcte et ne change pas le comportement. -->

        <!-- Guidage GPS : geo: et google.navigation: -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
        <!-- Repli web de l'itinéraire -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <!-- Appeler le client ou le vendeur -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <!-- Envoyer un SMS au client -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="sms"/>
        </intent>
        <package android:name="com.google.android.apps.maps"/>
    </queries>
</manifest>
